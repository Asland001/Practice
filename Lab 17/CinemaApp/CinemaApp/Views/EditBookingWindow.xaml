<Window x:Class="CinemaApp.Views.EditBookingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование брони" Height="370" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="12" Padding="16">
            <StackPanel>
                <TextBlock Text="Редактирование брони" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                <TextBlock Text="Фильм" FontWeight="Bold"/>
                <TextBox Text="{Binding MovieName}" IsReadOnly="True" Margin="0,4,0,10"/>

                <TextBlock Text="Время сеанса" FontWeight="Bold"/>
                <TextBox Text="{Binding SessionTime}" IsReadOnly="True" Margin="0,4,0,10"/>

                <TextBlock Text="Количество мест" FontWeight="Bold"/>
                <TextBox Text="{Binding SeatCount, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>

                <TextBlock Text="Покупатель" FontWeight="Bold"/>
                <TextBox Text="{Binding BuyerLogin, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,16"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Сохранить" Width="90" Margin="0,0,8,0" Click="Save_Click"/>
                    <Button Content="Отмена" Width="80" Click="Cancel_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>