<Window x:Class="CinemaApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CinemaApp.ViewModels"
        Title="Вход в систему" Height="300" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="16">
        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="12" Padding="16">
            <StackPanel>
                <TextBlock Text="Авторизация" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

                <TextBlock Text="Логин"/>
                <TextBox x:Name="LoginBox"
                         Height="28"
                         Margin="0,4,0,10"
                         Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Пароль"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="28"
                             Margin="0,4,0,16"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Регистрация"
                            Width="110"
                            Margin="0,0,8,0"
                            Click="Register_Click"/>
                    <Button Content="Войти"
                            Width="90"
                            Click="Login_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>